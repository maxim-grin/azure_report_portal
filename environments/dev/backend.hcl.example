# Copy to backend.hcl (gitignored, never committed) and fill in your
# values, then run: terraform init -backend-config=backend.hcl

resource_group_name  = "rg-tfstate"
storage_account_name = "yourstateaccount" # must be globally unique
container_name       = "tfstate"
key                  = "report-portal-dev.terraform.tfstate"
